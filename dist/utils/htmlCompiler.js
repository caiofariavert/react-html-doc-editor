// Funções para compilar/exportar HTML customizado
export function compileToCustomHTML(content) {
    // Implementação futura: converter estrutura do editor para HTML customizado
    return '<div>{{ conteudo }}</div>';
}
